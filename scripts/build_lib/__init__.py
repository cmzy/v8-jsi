"""Cross-platform build helpers for v8-jsi.

The PowerShell scripts in this directory are kept around for back-compat with
existing CI pipelines; new contributors should drive everything through the
top-level ``dev.py`` entry point, which is implemented on top of this package.
"""
